const styles = theme => ({
  flexToolbar: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center"
  },
  recipientContainer: {
    width: "100%"
  }
});

export default styles;
