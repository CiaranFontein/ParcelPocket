const styles = theme => ({
  flexToolbar: {
    display: "flex",
    justifyContent: "space-between"
  },
  appBar: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: theme.spacing(2)
  }
});

export default styles;
